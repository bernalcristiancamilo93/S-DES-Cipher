import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'S-DES-Cipher',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
